type LongWeapon {
    _id: ID,
    name: String,
    cost: Int,
    unlockCondition: String,
    ammunition: String,
    capacity: String,
    damage: Int,
    rateOfFire: Int,
    reloadSpeed: Int,
    meleeDamage:  Int,
    effectiveRange: Int,
    handling: Int,
    muzzleVelocity: Int,
    heavyMeleeDamage: Int,
    }

type Query {
    getLongWeapon(id: ID):LongWeapon,
    getLongWeapons:[LongWeapon],
    numberSix: Int
}